/*import { comodImovel } from "../entidades/comodImovel"
import { model, Schema } from 'mongoose'

const comodSchema = new Schema<comodImovel>({
    arCond: {type: Boolean, required: true},
    wifi: {type: Boolean, required: true},
    cozinha: {type: Boolean, required: true},
    garagem: {type: Boolean, required: true}
})

export const comodModel = model<comodImovel>('comod', comodSchema, 'comodidades')*/
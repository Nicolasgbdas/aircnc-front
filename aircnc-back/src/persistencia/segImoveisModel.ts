/*import { segImoveis } from "../entidades/segImovel"
import { model, Schema } from 'mongoose'

const segSchema = new Schema<segImoveis>({
    detecFumaça: {type: Boolean, required: true},
    extintorIncend: {type: Boolean, required: true},
    cameras: {type: Boolean, required: true},
    alarmcorbono: {type: Boolean, required: true}
})

export const segImoveisModel = model<segImoveis>('seg', segSchema, 'segurançaImoveis')*/